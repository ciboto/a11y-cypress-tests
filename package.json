{
  "name": "a11y-cypress-tests",
  "version": "1.0.0",
  "description": "Testes de acessibilidade com Cypress e cypress-axe",
  "scripts": {
    "test": "cypress run",
    "report:merge": "count=$(ls cypress/reports/json/*.json 2>/dev/null | wc -l) && if [ $count -gt 0 ]; then mochawesome-merge cypress/reports/json/*.json > cypress/reports/json/mochawesome.json; else echo 'Nenhum arquivo JSON válido para mesclar'; fi",
    "report:generate": "marge cypress/reports/json/mochawesome.json --reportDir cypress/reports/html --reportFilename report-test-accessibility-index",
    "report": "npm run report:merge && npm run report:generate"
  },
  "devDependencies": {
    "axe-core": "^4.10.3",
    "cypress": "^14.5.3",
    "cypress-axe": "^1.6.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}